import React from "react";
import { Link } from "react-router-dom";

const Signup = () => {
  return <div>
    <button>
        <Link to='/'>
            go to home page
        </Link>
    </button>
  </div>;
};

export default Signup;
